require "spec_helper"

describe ViewingViewTool do
  it "has a version number" do
    expect(ViewingViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
